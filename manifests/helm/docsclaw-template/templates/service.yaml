apiVersion: v1
kind: Service
metadata:
  name: {{ include "docsclaw-template.name" . }}
  labels:
    {{- include "docsclaw-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "docsclaw-template.selectorLabels" . | nindent 4 }}
