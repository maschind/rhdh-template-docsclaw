apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ include "docsclaw-build.name" . }}-external-git-token
  labels:
    {{- include "docsclaw-build.labels" . | nindent 4 }}
spec:
  secretStoreRef:
    name: vault-secret-store
    kind: ClusterSecretStore
  target:
    name: {{ include "docsclaw-build.name" . }}-git-token
  data:
  - secretKey: token
    remoteRef:
      key: secrets/rhdh/gitlab
      property: token
