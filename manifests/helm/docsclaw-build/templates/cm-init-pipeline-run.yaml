apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "docsclaw-build.name" . }}-trigger-pipeline
  labels:
    {{- include "docsclaw-build.labels" . | nindent 4 }}
data:
  trigger-pipeline.yaml: |
    apiVersion: tekton.dev/v1
    kind: PipelineRun
    metadata:
      generateName: {{ include "docsclaw-build.name" . }}-build-
      labels:
        {{- include "docsclaw-build.labels" . | nindent 8 }}
        backstage.io/kubernetes-id: {{ include "docsclaw-build.name" . }}
    spec:
      params:
        - name: source-repo
          value: {{ .Values.git.repo }}
        - name: source-branch
          value: {{ .Values.git.branch }}
        - name: output-image
          value: {{ include "image.dev-url" . }}
        - name: component-id
          value: {{ include "docsclaw-build.name" . }}
      pipelineRef:
        name: {{ include "docsclaw-build.name" . }}-build
      taskRunTemplate:
        serviceAccountName: pipeline
      timeouts:
        pipeline: 1h0m0s
      workspaces:
        - name: source-folder
          volumeClaimTemplate:
            spec:
              accessModes:
                - ReadWriteOnce
              resources:
                requests:
                  storage: 1Gi
{{- if eq .Values.image.registry "Quay" }}
        - name: docker-config
          secret:
            secretName: {{ include "docsclaw-build.name" . }}-registry-creds
{{- else }}
        - name: docker-config
          emptyDir: {}
{{- end }}
