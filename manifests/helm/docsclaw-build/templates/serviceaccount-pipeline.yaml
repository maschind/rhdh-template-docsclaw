{{- if eq .Values.image.registry "Quay" }}
kind: ServiceAccount
apiVersion: v1
metadata:
  name: pipeline
secrets:
  - name: {{ include "docsclaw-build.name" . }}-registry-creds
imagePullSecrets:
  - name: {{ include "docsclaw-build.name" . }}-registry-creds
{{- end }}
